{
  "name": "@sls-tf/extension-stack",
  "version": "0.1.0",
  "description": "Generate a companion CloudFormation stack from an sls.tf extension sidecar, so a SAM deploy can be completed rather than silently missing its extensions",
  "main": "src/generate.js",
  "private": true,
  "bin": {
    "extension-stack": "./bin/extension-stack.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
