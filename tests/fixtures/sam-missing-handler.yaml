AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: SAM template with a function missing its Handler

Resources:
  BrokenFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs18.x
      # Handler is intentionally omitted to trigger validation error
