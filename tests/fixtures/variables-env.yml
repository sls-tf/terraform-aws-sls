service: test-env-variables

provider:
  name: aws
  runtime: nodejs18.x
  stage: ${env:STAGE}
  region: ${env:AWS_REGION, 'us-east-1'}

functions:
  hello:
    handler: handler.hello
    environment:
      NODE_ENV: ${env:NODE_ENV}
      API_KEY: ${env:API_KEY, 'default-key'}
